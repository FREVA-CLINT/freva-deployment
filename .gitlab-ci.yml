stages:
    - doc


pages:
  stage: doc
  tags:
    - sphinx
  script:
    - cd docs && make html
  before_script:
    - python3 -m pip install .[docs]
  artifacts:
    when: always
    paths:
      - docs/build/html